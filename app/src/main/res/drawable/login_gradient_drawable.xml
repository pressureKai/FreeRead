<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 渐变色 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/login.background.center"
        android:endColor="@color/login.background.end"
        android:startColor="@color/login.background"
        android:type="linear"/>
</shape>